# ==============================================================
#  VulnScan — Python Dependencies
#  Install with:  pip install -r requirements.txt
# ==============================================================

# ── Web Framework (Backend API + WebSocket server) ──────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# ── HTTP Client (NVD CVE API calls) ─────────────────────────────
requests>=2.31.0
urllib3>=2.0.0

# ── Environment Variables (.env file loading) ───────────────────
python-dotenv>=1.0.0

# ── Google Gemini AI (primary SDK — preferred) ──────────────────
google-genai>=0.8.0

# ── Google Generative AI (legacy fallback SDK) ──────────────────
#    Only needed if google-genai fails to install on your system.
#    Uncomment the line below in that case:
# google-generativeai>=0.8.0

# ── Packet Analysis (optional — needed only for PCAP analysis) ──
#    Requires libpcap on Linux/macOS or Npcap on Windows.
#    Comment this out if you don't need PCAP support.
scapy>=2.5.0
